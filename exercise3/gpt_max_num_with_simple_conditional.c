//Конечно, вот пример нахождения максимального числа без использования тернарного
//оператора: 
 

#include <stdio.h>

int main() {
    int num1, num2, num3, max;
    
    printf("Введите первое число: ");
    if (scanf("%d", &num1) != 1) {
        printf("Ошибка при вводе первого числа.\n");
        return 1;
    }

    printf("Введите второе число: ");
    if (scanf("%d", &num2) != 1) {
        printf("Ошибка при вводе второго числа.\n");
        return 1;
    }

    printf("Введите третье число: ");
    if (scanf("%d", &num3) != 1) {
        printf("Ошибка при вводе третьего числа.\n");
        return 1;
    }
    
    max = num1;

    if (num2 > max) {
        max = num2;
    }

    if (num3 > max) {
        max = num3;
    }
    
    printf("Максимальное число: %d\n", max);
    
    return 0;
}

/*
В этом варианте мы просто сравниваем каждое число с текущим максимальным и, если
оно больше, присваиваем его в переменную  max. Это более прямой и читаемый
способ сравнения чисел, особенно если нет необходимости использовать тернарные
операторы. 
*/
